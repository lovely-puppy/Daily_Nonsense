### 周三
2026年05月06日

  **说说你对Spring事务的理解**
  > Spring 事务通过 AOP 动态代理实现，在方法前后自动管理事务的开启、提交和回滚。加 @Transactional 注解即可，默认传播行为是 REQUIRED（有事务就加入，没有就新建），默认只回滚 RuntimeException。

>事务失效的常见场景有五种：方法非 public 代理拦截不到、同类内部调用走 this 不走代理、异常被 catch 吞了 Spring 感知不到、抛 Checked Exception 默认不回滚需要加 rollbackFor、数据库引擎不支持事务。